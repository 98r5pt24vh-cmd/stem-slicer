# Matches the validated arm64 Qt runtime in the existing macOS application
# and is also available for the Windows CPython 3.12 runner.
PySide6==6.9.3
pyinstaller==6.18.0
# Stem Slicer vendors the small Basic Pitch ONNX inference code and model.
# Pin only its actual ONNX runtime dependencies; installing the upstream
# metapackage would also pull unused TensorFlow/Core ML engines into the build.
librosa==0.11.0
mir-eval==0.8.2
numba==0.60.0
numpy==1.26.4
onnxruntime==1.19.2
pretty_midi==0.2.11
resampy==0.4.2
scipy==1.13.1
setuptools==80.9.0
