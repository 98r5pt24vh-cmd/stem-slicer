# Qt 6.11.1 includes the macOS CoreAudio fix that prevents QMediaPlayer from
# changing the hardware device sample rate globally (QTBUG-145793).
# Build this release with CPython 3.12 arm64; do not downgrade this pin by
# copying the runtime from the 1.8B application bundle.
PySide6==6.11.1
pyinstaller==6.18.0
# Stem Slicer vendors the small Basic Pitch ONNX inference code and model.
# Pin only its actual ONNX runtime dependencies; installing the upstream
# metapackage would also pull unused TensorFlow/Core ML engines into the build.
librosa==0.11.0
mir-eval==0.8.2
numba==0.60.0
numpy==1.26.4
onnxruntime==1.19.2
pretty_midi==0.2.11
resampy==0.4.2
scipy==1.13.1
soundfile==0.14.0
soxr==1.1.0
setuptools==80.9.0
# Generate classifier runtime.  These pins match the MERT corpus artifact and
# are required inside the packaged app for first-time library scans.
torch==2.5.1
transformers==4.38.2
huggingface_hub==0.36.2
safetensors==0.8.0
joblib==1.5.3
scikit-learn==1.9.0
